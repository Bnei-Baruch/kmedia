.modal-header
  a.close data-dismiss='modal' &times;
  h3 Mark containers to merge into the current one...
.modal-body
  = form_for ['merge_admin', @container], method: :post, html: { class: 'well form-vertical' } do |f|
    .control-group.check_boxes.optional.lesson_container_ids
      - @containers.all.each do |container|
        .controls.error
          label.checkbox
            = check_box_tag 'container[container_ids][]', container.id, class: 'check_boxes optional'
            = container.name
            = hidden_field_tag 'container[container_ids][]'
    .modal-footer
      button.btn.btn-danger type='submit' href='#' Merge
      a.btn href='#' data-dismiss='modal' Close
